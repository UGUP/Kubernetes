apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      batch: c38
  template:
    metadata:
      name: nginx
      labels:
        batch: c38
    spec:
      containers:
      - name: voting
        image: dipesh017/demo:vote
        ports:
          - containerPort: 3000
        

     
    